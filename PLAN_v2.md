# v2 方案：以优化驱动的迭代升级

> **核心理念**: 不复制别人方案。以 v1（0.94918）为基座，逐项修复短板，每个改动都用实验验证，只保留有收益的优化。
> **目标**: 本地 OOF ≥ 0.9505，Kaggle Public ≥ 0.9505

---

## 一、优化思路（不是复制，是验证）

v1 的 LGBM + CatBoost 已经验证了可行性。v2 做**受控优化实验**：

```
v1 基线 (80/20 split, LGBM+CB 等权) = 0.9497 本地
    │
    ├─ 优化 1: 验证框架升级 80/20 → 5-fold OOF
    │   目的: 获得 OOF 概率，解锁后续所有融合优化
    │
    ├─ 优化 2: 单种子 → 多种子 (42/2026/3407)
    │   假设: 降方差。用 OOF 验证是否真有提升，没有就回退
    │
    ├─ 优化 3: 超参数优化 (对我们的 LGBM/CB 调参)
    │   工具: Optuna，在 OOF 上评估，不过拟合
    │
    ├─ 优化 4: 融合权重优化 (OOF 上搜索最优权重)
    │   对比: 等权 vs 优化权重，差值 <0.0002 就保持简单
    │
    └─ 优化 5: 特征消融测试 (目标编码是否对"我们"的管道有效)
        方法: A/B 测试，别人有效≠我们有效，用数据说话
```

**决策原则**: 每项优化单独评估，OOF 提升 ≥0.0002 才保留，否则回退。避免为了复杂而复杂。

---

## 二、执行步骤

### 阶段 1: 基础设施（数据 + 验证框架）
1. 拷贝数据到 v2/data/
2. 实现 fold-local 预处理（防泄漏）
3. 实现 5-fold StratifiedKFold OOF 训练框架

### 阶段 2: 复现 v1 基线（在 OOF 框架下）
4. 用 OOF 框架重跑 v1 的 LGBM + CatBoost，确认基线 OOF ≈ 0.9497
5. 记录为基准点

### 阶段 3: 逐项优化实验
6. **实验 A**: 多种子 LGBM（3 seeds）→ 对比单种子
7. **实验 B**: Optuna 调参 LGBM → 对比默认参数
8. **实验 C**: Optuna 调参 CatBoost → 对比默认参数
9. **实验 D**: 融合权重优化 → 对比等权
10. **实验 E**: TargetEncoder 消融 → A/B 测试有效性

### 阶段 4: 最终融合与提交
11. 用保留的最优组件构建最终融合
12. 生成 submission.csv + 格式校验
13. 记录 EXPERIMENTS.md 实验报告

---

## 三、实验记录模板

每个实验记录：
```
| 实验 | 改动 | OOF BA | 相比基线 | 决策 |
|------|------|:---:|------|------|
| 基线 | v1 LGBM+CB | 0.94xx | — | 基准 |
| A | +多种子 | ? | +? | 保留/回退 |
| B | +LGBM调参 | ? | +? | 保留/回退 |
| ... | ... | ... | ... | ... |
```

只保留有正收益的改动，最终方案 = 基线 + 所有保留的优化。

---

## 四、技术栈

pandas, numpy, scikit-learn, LightGBM, CatBoost, Optuna（调参）, 5-fold StratifiedKFold

**资源约束**: 全程控制内存/时间，单模型 fold 训练 < 5 分钟，总时长 < 60 分钟。如某实验超时，用 50% 数据采样做快速验证。

---

**状态**: 已确认，开始执行。
